import { TFormStatus } from 'modules/forms/config/types';

const status: TFormStatus = {
  title: 'Новый статус веб-формы',
  description: '',
  sort: 100,
  isActive: true,
  isDefault: false,
};

export default status;
